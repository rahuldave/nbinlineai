{
  "title": "nbinlineai",
  "description": "AI prompt cell settings for response style, model, and tool steps. ChatGPT sign-in and API keys are configured through Configure AI; credentials are never saved in notebooks.",
  "type": "object",
  "properties": {
    "defaultBackend": {
      "type": "string",
      "title": "Default provider",
      "enum": [
        "openai_api",
        "anthropic_api",
        "openai_codex_subscription"
      ],
      "default": "openai_api"
    },
    "backendModels": {
      "type": "object",
      "title": "Model for each connection (blank uses server default)",
      "properties": {
        "openai_api": {
          "type": "string",
          "default": ""
        },
        "anthropic_api": {
          "type": "string",
          "default": ""
        },
        "openai_codex_subscription": {
          "type": "string",
          "default": ""
        }
      },
      "additionalProperties": false,
      "default": {
        "openai_api": "",
        "anthropic_api": "",
        "openai_codex_subscription": ""
      }
    },
    "maxToolSteps": {
      "type": "integer",
      "title": "Maximum tool steps",
      "minimum": 0,
      "maximum": 10,
      "default": 5
    },
    "promptMode": {
      "type": "string",
      "title": "Response style",
      "description": "User fallback for notebooks and cells without a style override. Compact is succinct, Full gives detailed explanations and code, and Learning guides with questions and at most three lines of code without full solutions.",
      "enum": [
        "compact",
        "full",
        "learning"
      ],
      "default": "compact"
    },
    "promptInstructions": {
      "type": "object",
      "title": "Custom response instructions",
      "description": "Optional per-style instructions; blank uses the server default. Maximum 8000 characters per style.",
      "properties": {
        "compact": {
          "type": "string",
          "maxLength": 8000,
          "default": ""
        },
        "full": {
          "type": "string",
          "maxLength": 8000,
          "default": ""
        },
        "learning": {
          "type": "string",
          "maxLength": 8000,
          "default": ""
        }
      },
      "additionalProperties": false,
      "default": {
        "compact": "",
        "full": "",
        "learning": ""
      }
    }
  },
  "additionalProperties": false
}
